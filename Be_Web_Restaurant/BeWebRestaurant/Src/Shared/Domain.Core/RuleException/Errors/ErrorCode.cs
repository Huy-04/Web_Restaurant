namespace Domain.Core.RuleException.Errors
{
    public enum ErrorCode
    {
        ValidationFailed,
        NotFound,
        Conflict,
        Unauthorized,
        Forbidden,
        InternalServerError
    }
}